pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        //id 'org.podval.tools.scalajs' version '0.9.14'
    }
}

rootProject.name = 'djc-crm'

include ':scala_js', ':frontend', ':backend', ':auth_service'